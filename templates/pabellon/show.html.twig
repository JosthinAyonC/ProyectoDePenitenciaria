{% extends 'base.html.twig' %}

{% block title %}Pabellon{% endblock %}

{% block body %}
{{parent() }}
    <h1>Pabellon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPabellon</th>
                <td>{{ pabellon.idPabellon }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pabellon.nombre }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ pabellon.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pabellon_index') }}">back to list</a>

    <a href="{{ path('app_pabellon_edit', {'idPabellon': pabellon.idPabellon}) }}">edit</a>

    {{ include('pabellon/_delete_form.html.twig') }}
{% endblock %}
